<%= form_for(@bipd) do |f| %>
  <% if @bipd.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bipd.errors.count, "error") %> prohibited this bipd from being saved:</h2>

      <ul>
      <% @bipd.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :medical %><br>
    <%= f.text_field :medical %>
  </div>
  <div class="field">
    <%= f.label :submission_id %><br>
    <%= f.text_field :submission_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
